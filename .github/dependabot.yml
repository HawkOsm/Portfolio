version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: monthly
    groups:
      # one tidy PR per month instead of ten
      all-dependencies:
        patterns:
          - '*'
